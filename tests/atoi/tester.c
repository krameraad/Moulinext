#include "../libft.h"
#include "../utils.h"

int main(int argc, char const *argv[])
{
    int expected;
    int received;

    CREATE_BUF;
    if (ft_atoi(buf) != atoi(buf))
        return 1;
    return 0;
}
